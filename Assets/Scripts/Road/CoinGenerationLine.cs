using Assets.Scripts.Tools;
using UnityEngine;


public class CoinGenerationLine : MonoBehaviour
{

    // The range of track indices where the line can be placed. For example, if trackIndexRange is (0, 2), the line can be placed on tracks 0, 1, and 2.
    public IntRange TrackIndexRange = new(0, 0);
    public FloatRange ZAxisOffsetRange = new();

    private void Awake()
    {
        
    }

    private void Start()
    {
        
    }

    private void Update()
    {
        
    }
}
